<h1>Merritt - Ingest Workspace Service</h1>
<div>

<h2>Sample Object Listing</h2>
<p>This page will display sample objects / top level prefixes to help identify the keys within the bucket.</p>

<% if @listing.topobjlist %>
  <h3>Top Level Objects</h2>
  <ul>
    <% @listing.topobjlist.each do |k| %>
      <li><%= k[:key] %></li>
    <% end %>
  </ul>
<% end %>

<% if @listing.prefixes %>
  <h3>Top Level Prefixes</h2>
  <ul>
    <% @listing.prefixes.each do |k| %>
      <li><%= k[:key] %></li>
    <% end %>
  </ul>
<% end %>
</div>
