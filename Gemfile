source 'http://rubygems.org'
ruby '~> 3.2'

gem 'sinatra'
gem 'json'
gem 'rack'
gem 'rack-contrib'
gem 'rubysl-base64'
gem 'aws-sdk-s3'
gem 'aws-sdk-ssm'

# These are the dependencies that are used only for unit tests.
group :test do
  gem "rspec"
  gem "rack-test"
end
