source 'http://rubygems.org'
ruby '~> 2.7'

gem 'sinatra'
gem 'json'
gem 'rack'
gem 'rack-contrib'
gem 'rake'
gem 'rubysl-base64'
gem 'aws-sdk-s3'
gem 'aws-sdk-ssm'
gem 'aws-sdk-sts'
gem 'aws-sdk-route53'

# These are the dependencies that are used only for unit tests.
group :test do
  gem "rspec"
  gem "rack-test"
end
